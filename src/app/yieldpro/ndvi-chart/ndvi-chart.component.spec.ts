import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NdviChartComponent } from './ndvi-chart.component';

describe('NdviChartComponent', () => {
  let component: NdviChartComponent;
  let fixture: ComponentFixture<NdviChartComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
    imports: [NdviChartComponent]
});
    fixture = TestBed.createComponent(NdviChartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NdviChartComponent } from './ndvi-chart.component';

describe('NdviChartComponent', () => {
  let component: NdviChartComponent;
  let fixture: ComponentFixture<NdviChartComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
    imports: [NdviChartComponent]
});
    fixture = TestBed.createComponent(NdviChartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NdviChartComponent } from './ndvi-chart.component';

describe('NdviChartComponent', () => {
  let component: NdviChartComponent;
  let fixture: ComponentFixture<NdviChartComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
    imports: [NdviChartComponent]
});
    fixture = TestBed.createComponent(NdviChartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

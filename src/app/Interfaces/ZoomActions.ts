
export default interface ZoomActions {
    range: number[];
    // this is a function that adds and removes layers
    add: () => void;
    currentLevel: string;
  }

export default interface ZoomActions {
    range: number[];
    // this is a function that adds and removes layers
    add: () => void;
    currentLevel: string;

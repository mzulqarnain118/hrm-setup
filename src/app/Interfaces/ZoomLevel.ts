export default interface IZoomLevel {
    zoom_level: number;
    next_zoom: number;
    add: string[];
    rem: string[];
  }
export default interface IZoomLevel {
    zoom_level: number;
    next_zoom: number;
    add: string[];
    rem: string[];

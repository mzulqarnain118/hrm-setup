export interface MapConfig {
  source: string;
  zoom: number;
atLong?: mapboxgl.LngLat;
  color?: string;
}
export interface MapConfig {
  source: string;
  zoom: number;
  latLong?: mapboxgl.LngLat;
  color?: string;
}

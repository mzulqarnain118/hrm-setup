import { ComponentFixture, TestBed } from '@angular/core/testing';

import { T3BarChartComponent } from './t3-bar-chart.component';

describe('T3BarChartComponent', () => {
  let component: T3BarChartComponent;
  let fixture: ComponentFixture<T3BarChartComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
    imports: [T3BarChartComponent]
});
    fixture = TestBed.createComponent(T3BarChartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { T3BarChartComponent } from './t3-bar-chart.component';

describe('T3BarChartComponent', () => {
  let component: T3BarChartComponent;
  let fixture: ComponentFixture<T3BarChartComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
    imports: [T3BarChartComponent]
});
    fixture = TestBed.createComponent(T3BarChartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
